{
  "name": "trpg-ai-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "generate-token-hashes": "node scripts/generate-token-hashes.mjs",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsx": "^4.22.3",
    "typescript": "^5.8.3",
    "vitest": "^4.0.0"
  }
}
